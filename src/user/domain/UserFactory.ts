import { Uuid } from 'src/shared/domain/Uuid';
import { User } from './User';
import { UserEmail } from './UserEmail';
import { UserName, UserNameTypes } from './UserName';
// import { UserPassword } from './UserPassword';

export class UserFactory {
  // create(
  //   firstName: string,
  //   lastName: string,
  //   email: string,
  //   password?: string,
  //   id?: string,
  // ): User {
  //   return new User(
  //     Uuid.create(id),
  //     UserName.create(firstName, UserNameTypes.FIRST),
  //     UserName.create(lastName, UserNameTypes.LAST),
  //     UserEmail.create(email),
  //     UserPassword.create(password),
  //   );
  // }
}
